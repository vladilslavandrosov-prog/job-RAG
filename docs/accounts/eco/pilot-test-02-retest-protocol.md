# Протокол ре-теста пилота СМК — АО «Эко Ресурс»

**Дата ре-теста:** 12.09.2026
**Базовый тест:** пилот от 11.09.2026, 18 вопросов, разбор — `pilot-test-01-analysis.md`
**Метод:** те же вопросы задаются повторно в чате платформы теми же формулировками; ответ и источники сверяются с исходным прогоном.
**Статус документа:** снимок состояния «как есть» на дату/время каждой записи ниже (не финальный вывод). План: зафиксировать текущее состояние по всем 18 вопросам → почистить/перезалить корпус загруженных документов → повторный тест с отдельным протоколом (`pilot-test-03`).

## Условия проведения ре-теста (важно для воспроизводимости)

Зафиксировано по скриншоту интерфейса платформы на 12.09.2026, 13:44–13:46:

| Параметр | Значение |
|---|---|
| Модель | **OpenAI/gpt-5.5** |
| Агент/режим | **«Синапс.Инсайт — Ана...»** (название обрезано в интерфейсе на скриншоте, точное полное название не подтверждено) + переключатель «АГЕНТ» включён |
| Рассуждения (reasoning) | **Выключены** («Рассуждения ВЫКЛ») |
| Область поиска — источники | ☑ «Данные (каталог и SQL)», ☑ «Документы», ☐ «Интернет» (отмечен как «ВНЕШНИЙ», не использовался) |
| Область поиска — коллекции | ☑ только **«ЭКО»** — сужено до 1 коллекции («Данные и документы · сужено: 1») |
| Периметр | Отмечен бейджем «🔒 защищённый контур — данные не покидают компании» |

**Почему это важно:** ни исходный пилот от 11.09, ни этот ре-тест не документировали эти параметры явно до сих пор — если модель, режим агента или область поиска отличались между 11.09 и 12.09, часть найденных изменений («исправлено»/«регрессия»/«без изменений») может объясняться разницей в условиях запуска, а не изменениями в самом корпусе документов или парсере. **Нужно уточнить у заказчика, были ли эти параметры идентичны 11.09 и 12.09** — без этого сравнение «было/стало» справедливо только при допущении, что условия не менялись.

Отдельно значимо, что область поиска включает **«Данные (каталог и SQL)», а не только «Документы»** — это значит, что задействован не только RAG-механизм (домены/коллекции), но потенциально и структурный SQL-слой (если у «Эко» есть такие источники — см. открытый вопрос в `access-audit.md`). Стоит уточнить, влияет ли эта настройка на данный конкретный тест (все вопросы пилота — про регламенты, не про табличные данные) или это просто общая настройка агента по умолчанию.

## 1. Контекст

В разборе исходного пилота (`pilot-test-01-analysis.md`) 5 из 7 подтверждённых ошибок были связаны с одной вероятной причиной: пять источников лежали в старом формате `.doc`, который не открылся ни в LibreOffice, ни в pandoc при независимой проверке. Была выдвинута гипотеза: если пайплайн загрузки документов платформы использует похожую конвертацию, эти документы могли не проиндексироваться или проиндексироваться пусто — отсюда ответы «не найдено» там, где текст реально присутствовал в файле.

12.09.2026 пользователь начал повторно задавать те же вопросы в чате платформы. По состоянию на текущую версию протокола перепроверены **17 из 18 вопросов**: **3 из 7 ошибок пилота исправлены, 1 частично улучшена, 2 без изменений**, и параллельно обнаружена **1 регрессия** на вопросе, который в исходном пилоте отвечался верно, плюс **паттерн из 2 аномалий цитирования источника**. Картина смешанная, не однозначно позитивная.

## 2. Исправлено

| № | Вопрос | Было (11.09) | Стало (12.09) | Источник при ре-тесте |
|---|---|---|---|---|
| 1.2 | Я технолог ЛР. Кто управляет процессом, когда мы делаем экспериментальную партию? | «Нет чёткого ответа» — ответственный не найден | **Отвечено верно**: «Экспериментальной выработкой руководит технолог лаборатории: он управляет производственным процессом и при необходимости вносит корректировки в технологию с отражением их в РИ (Форма 16-03). После завершения процесса образец передают в ОКК; при отклонении качества продукт дорабатывается под руководством технолога лаборатории.» | ПР-03-00-12.doc — 53%; **И-16-01-11.doc — 41%** |
| 1.3 | Я мастер на производстве. В заказе увидел пометку «Экспериментальная». Мои действия? | «Частичный ответ» — специальные действия не найдены | **Отвечено верно**: «При пометке «Экспериментальная» выполняйте выработку по указаниям технолога лаборатории — он руководит экспериментальным производственным процессом и при необходимости корректирует технологию. Самостоятельно менять технологию не следует; изменения должны быть отражены в РИ (Форма 16-03). После завершения образец передают в ОКК.» | И-09-08-02.doc — 61%; **И-16-01-11.doc — 60%**; И-15-06-08.docx — 57%; И-08-02-10.doc — 56% |
| 5.1 | Я технолог производства. Сырьё на складе «Проверка ОКК». Как получить? | «Частичный ответ» — «прямого порядка... нет» (был открытым вопросом, не в числе 7 ошибок) | **Полный пошаговый ответ** (5 шагов) именно для склада «Проверка ОКК» | ПР-09-00-14.doc — 49%; ПР-03-00-12.doc — 65% |

Дополнительно перепроверен вопрос 1.1 (не входил в число ошибок пилота) — ответ не изменился, платформа по-прежнему отвечает верно, источник тот же (ПР-03-00-12.doc, 60%).

## 3. Частично улучшено

| № | Вопрос | Было (11.09) | Стало (12.09) | Источник при ре-тесте |
|---|---|---|---|---|
| 3.2 | Клиент попросил поменять характеристику товара, заявка на сборку уже распечатана | «Частичный ответ» — что делать с распечатанной заявкой не описано; источник И-12-10-03 числился «не загружен в корпус» | Источник теперь подключён, появился шаг именно про распечатанную заявку, но точная инструкция «перепровести Заявку на комплектацию» из разбора ошибок всё ещё не дана | И-12-10-03.docx — 64% |

## 4. Регрессия — вопрос, который раньше отвечался верно, а теперь нет

| № | Вопрос | Было (11.09) | Стало (12.09) | Источник при ре-тесте |
|---|---|---|---|---|
| 2.1 | Какие поля заполняет оператор после фасовки продукции? | **Чёткий ответ**: «После фасовки оператор ПО заполняет в «Рабочей инструкции на производство» (форма 16-03): «Кол-во тарных мест», «Общее количество», «Дата», «Фасовщик ФИО подпись».» | **Регрессия**: «Какие именно поля оператор заполняет после фасовки, в найденном фрагменте не указано.» Названы только поля, проверяемые ДО фасовки. | Текст ответа ссылается на И-08-02-10 и ПР-08-00-14, но карточка источника в чате показывает **другой файл — `Datafortest07.07.xlsx`**, не входящий в известный корпус СМК |

Это не «ещё не перепроверено» — это ухудшение относительно исходного пилота: вопрос, который платформа раньше отвечала полностью и точно, сейчас отвечает хуже.

## 5. Без изменений — ошибки, которые остались ошибками

| № | Вопрос | Было (11.09) | Стало (12.09) | Источник при ре-тесте |
|---|---|---|---|---|
| 2.2 | Корректировка Заказа на производство при меньшем весе | «Частичный ответ» — только общая процедура для Заказа покупателя | **Без изменений**: тот же общий ответ. Конкретная процедура (Д-08-04-01, п. 3.5.1–3.5.5) по-прежнему не дана | И-12-10-03.docx — 72%. **Д-08-04-01** по-прежнему не цитируется |
| 2.3 | Высота сброса тары 1,5–5 л | «Нет чёткого ответа» — высота не указана | **Без изменений**: высота (0,8 м) по-прежнему не дана | Текст называет Д-09-01-17, карточка показывает файл **«глоссарий»** (100%) — вторая аномалия цитирования подряд |

Отдельно любопытный факт по 2.2: источник `И-12-10-03.docx` — тот самый документ, который в исходном разборе пилота числился как «не загружен в корпус» (был нужен для вопроса 3.2). Судя по этой цитате, он на платформе уже есть — корпус на платформе, по-видимому, шире, чем то, что было прислано нам для локального анализа.

**Паттерн аномалии цитирования (2.1 и 2.3).** Дважды подряд карточка источника в интерфейсе чата показывает файл, не совпадающий с тем, что назван в тексте ответа («Datafortest07.07.xlsx» и «глоссарий»), оба раза с высоким процентом релевантности (64% и 100%). Это не похоже на случайный шум ранжирования — скорее на путаницу в том, что именно система показывает как «источник» в UI против того, что реально использовала модель. Это отдельный от `.doc`-парсинга риск, который стоит проверить перед тем, как чистить/перезаливать корпус.

## 6. Без изменений — верные ответы (не входили в число ошибок)

| № | Вопрос | Комментарий |
|---|---|---|
| 3.1 | Технолог ПО получает сырьё на производство | Без изменений, верно, источники релевантны, аномалий нет |
| 3.3 | Продукцию забраковали — действия | Без изменений, верно, ответ даже детальнее (формы 03-01/03-03, конкретные роли) |
| 4.1 | Марка стретч-плёнки | Без изменений — верное «не найдено» (детали физически нет в корпусе, подтверждено в `pilot-test-01-analysis.md` §5) |
| 4.2 | Периодичность оценки подрядчиков | Без изменений — вопрос остаётся открытым, найдено смежное поле «Оценка результативности», но не сама периодичность |
| 4.3 | Поля Формы 17-08 | Без изменений — верное «не найдено» (сама форма как бланк не в корпусе), источник цитирования сменился на другой файл |
| 5.3 | Запас продукции при занятом оборудовании | Без изменений, верно, ответ детальнее с постатейной сверкой источников |
| 6.1 | Действия оператора по фасовке | Без изменений, верно, ответ детальнее исходного |

## 7. Не перепроверено

| № | Вопрос | Ожидаемый источник (по пилоту) |
|---|---|---|
| 5.2 | Приоритет между заявками на ремонт | ПР-10(02)-00.docx — retrieval-случай, не парсинг — **уже перепроверен, см. отдельно ниже** |
| 6.2 | Добавление новой Спецификации в 1С-8 | И-08-05-08.doc |
| 6.3 | Процесс входного контроля сырья | не входил в число ошибок |

**Важное уточнение по 5.2 (контрольный retrieval-случай):** уже перепроверен — **без изменений**, по-прежнему «не найдено», источник вообще не показан. Это прямое подтверждение «Корневой причины №2» из `proverka-zdorovya-zagruzki-predlozhenie.md`: правки, починившие 1.2/1.3 (парсинг `.doc`), не затронули retrieval-проблему 5.2 — ровно как предсказывалось, поскольку причина другая.

## 8. Итоговая сводка на момент составления протокола

| Категория | Количество | Вопросы |
|---|---|---|
| Исправлено | 3 | 1.2, 1.3, 5.1 |
| Частично улучшено | 1 | 3.2 |
| Регрессия | 1 | 2.1 |
| Без изменений — ошибка сохраняется | 3 | 2.2, 2.3, 5.2 |
| Без изменений — верно (не были ошибками) | 7 | 1.1, 3.1, 3.3, 4.1, 4.2, 4.3, 6.1 |
| Не перепроверено | 2 | 6.2, 6.3 |

**Требует уточнения — важная деталь по исправленным вопросам.** В цитатах 1.2/1.3 источник указан как `И-16-01-11.doc` (расширение `.doc`, не `.docx`) — то есть починка произошла не через пересохранение в `.docx`, как предполагала рекомендация в `pilot-test-01-analysis.md`, а каким-то другим образом (переиндексация на стороне платформы, обновление парсера или что-то ещё). Случаи 2.1 (регрессия) и 2.2/2.3 (без изменений) показывают, что изменение не было равномерным «почини всё `.doc` сразу» — это точечная правка отдельных документов, а не системный фикс формата.

## 9. Рекомендация

1. **Срочно, до чистки/перезаливки корпуса** — разобраться, почему карточка источника дважды подряд показывала файл, не совпадающий с тем, что назван в тексте ответа (`Datafortest07.07.xlsx`, «глоссарий»). Это системный риск достоверности цитирования, а не проблема конкретных двух вопросов.
2. Уточнить у заказчика, были ли условия теста (модель `OpenAI/gpt-5.5`, область поиска, сужение до коллекции «ЭКО») идентичны 11.09 и 12.09 — иначе часть сравнений «было/стало» некорректна.
3. Завершить снимок текущего состояния по оставшимся 2 вопросам (6.2, 6.3), прежде чем чистить корпус.
4. По вопросу 2.2 — отдельно уточнить у команды платформы, почему `Д-08-04-01` не подтягивается, при том что рядом лежащий `И-12-10-03` уже работает (`.docx`).
5. Уточнить у команды платформы, что именно изменилось между 11.09 и 12.09 (переиндексация конкретных файлов, обновление парсера, что-то ещё) — без этого нельзя точно сформулировать причину ни исправлений, ни регрессии, ни отсутствия изменений.
6. После завершения снимка — почистить/перезалить корпус (см. `converted-for-upload/`) и повторить полный 18-вопросный тест заново, отдельным протоколом (`pilot-test-03`), сравнивая уже не с исходным пилотом 11.09, а с этим снимком 12.09.
7. `pilot-test-01-analysis.md` и обе презентации не трогать до завершения цикла «снимок → чистка → повторный тест».
