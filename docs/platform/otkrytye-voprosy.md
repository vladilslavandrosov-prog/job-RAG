# Сводный реестр открытых вопросов

Все ещё открытые вопросы из статей базы знаний, собранные в одном месте и сгруппированные по пунктам меню платформы — в порядке самого меню. По каждому вопросу — краткий контекст и варианты ответа (гипотезы на основе того, что уже известно из соседних экранов и находок), а не подтверждённые факты. Вопросы, на которые уже нашёлся ответ, из реестра убираются — актуальный факт остаётся в соответствующей статье (`chat.md`, `agenty.md` и т. д.) и в разборе кейса, где он появился.

Это черновик для сверки с реальной командой платформы, а не финальный источник истины.

---

## Чат

> См. [`chat.md`](chat.md). Вопросы про суффикс «veai» и про периметр данных при вызове `OpenAI/gpt-5.4` сняты — см. `docs/analysis/case-03-pilot-tz-ai-guard.md` (компонент AI Guard).

**В: Какие ещё модели доступны в этом селекторе, кроме `OpenAI/gpt-5.4`?**
- A. Локальные модели через Ollama (упомянуты в ТЗ, раздел 3.2)
- B. Другие облачные модели (Anthropic, другие провайдеры)
- C. Только одна эта модель — селектор чисто декоративный на момент снимка

**В: Что именно показывает переключатель «Рассуждения» — реальные шаги инструментов или только текст размышления модели?**
- A. Полный technical reasoning log (SQL-запросы, найденные чанки, план) — то же самое, что должно быть в «Отладке сессий»
- B. Только текстовое chain-of-thought рассуждение модели, без привязки к конкретным вызовам инструментов
- C. Оба слоя одновременно, переключатель просто показывает/скрывает весь блок целиком

**В: Что делает чекбокс со звездой рядом с «Агент»?**
- A. Добавить агента в «избранное» для быстрого доступа
- B. Отметить чат/сессию как избранную (не агента)
- C. Переключатель между «системным» и «канвасным» режимом агента

**В: Полный список доступных агентов/персон?**
- Открыт полностью — на скриншотах видно только «Платформа — Аналитик» (в чате) и три «Активных агента» (в разделе «Агенты»); неизвестно, совпадают ли это одни и те же сущности.

---

## Мои шаблоны

> См. [`moi-shablony.md`](moi-shablony.md)

**В: Что показывается на этой странице, если уже есть сохранённые шаблоны?**
- A. Список карточек/строк с уже созданными шаблонами (аналогично «Активным агентам» в разделе «Агенты»)
- B. Та же форма создания, но с вкладкой/переключателем «мои шаблоны» рядом
- Снимок показывал только пустую форму создания — не факт, что это единственный вид экрана.

**В: Как именно работает «recall» — векторное сходство, как обычный retrieval, или отдельный механизм?**
- A. Тот же векторный поиск, что и для документов, просто по отдельной коллекции «вопрос-триггер»
- B. Отдельный, более простой механизм (например, точное/нечёткое совпадение фраз, не векторное)
- C. Комбинация: сначала грубый фильтр по ключевым словам, затем векторное ранжирование кандидатов

**В: Что произойдёт при конфликте — вопрос похож сразу на два шаблона?**
- A. Побеждает шаблон с наивысшим сходством (score)
- B. Применяются оба (склеенный ответ) — маловероятно, но не исключено
- C. Правило приоритета по порядку создания/последнему изменению

**В: Какие типы графиков доступны в дропдауне «График»?**
- Открыт полностью — видно только значение по умолчанию «— нет —».

**В: Кто может создавать/редактировать шаблоны — любой пользователь или только куратор/админ?**
- A. Только куратор/аналитик с отдельной ролью (согласуется с описанием «курируемые образцы»)
- B. Любой пользователь для своих же будущих вопросов
- C. Роль зависит от типа шаблона (обычный vs строгий)

---

## Источники → Документы

> См. [`istochniki-dokumenty.md`](istochniki-dokumenty.md)

**В: Что означают все возможные значения «Тип» документа, кроме `Другое`, `Регламент`, `Политика`?**
- A. Полный список соответствует типам, используемым в «Предложениях» (`document_review`, `glossary_term` и т.д.) — маловероятно, это разные таксономии
- B. Отдельный, более простой список категорий по жанру документа (инструкция, положение, договор и т.п.)

**В: Что находится в меню «...» по каждой строке таблицы документов?**
- A. Просмотр фрагментов/чанков документа
- B. Повторная индексация конкретного документа
- C. Удаление, изменение видимости/домена
- D. Комбинация из нескольких пунктов выше

**В: Как выглядит статус, отличный от «Готово» (например, ошибка обработки)?**
- A. Аналогично статусу «Упавшие» из «Очереди обработки» — с той же цветовой индикацией (красный)
- B. Отдельный набор статусов специфичный для документов (например, «В очереди», «Обрабатывается», «Ошибка»)

**В: Полный список доменов и коллекций, кроме `Производство`/`AZ_tech`?**
- Открыт полностью — согласуется с находкой о мульти-доменности платформы (`zadachi-review-pipeline.md`): вероятно, есть домены и для брокерского направления, и для кредитного риска.

---

## Задачи

> См. [`zadachi-review-pipeline.md`](zadachi-review-pipeline.md)

**В: Что такое колонка «Паттерн» в «Предложениях», почему всегда пусто?**
- A. Поле для будущей функциональности (ещё не реализовано), пустое по умолчанию
- B. Заполняется только для определённых типов предложений (не встретившихся в примере)
- C. Технический паттерн/regex, которым был найден кандидат — просто не был заполнен в этих конкретных строках

**В: Расходятся ли «С ошибкой: 0» (плитка статистики) и «Ошибка индексации: 92» (разрез «по типу»)?**
- A. Это разные метрики: плитка считает ошибки при обработке самого предложения, «по типу» — предложения, которые сами являются записью об ошибке индексации источника
- B. Баг в отображении/агрегации статистики
- C. «С ошибкой» относится только к последним N предложениям, а «по типу» — ко всей очереди целиком

**В: Связаны ли 5 «упавших» задач из «Очереди обработки» с 92 «ошибками индексации» из «Статистики»?**
- A. Да, это одни и те же события на разных стадиях (упала задача в очереди → зафиксирована как «ошибка индексации» в предложениях)
- B. Нет, это независимые счётчики из разных систем/периодов
- C. Частичное пересечение — часть из 92 могла возникнуть раньше, чем нынешние 5 упавших

---

## Калибровка → Политики поиска

> См. [`kalibrovka-politiki-poiska.md`](kalibrovka-politiki-poiska.md)

**В: Полный текст regex-паттернов интентов (обрезаны на скриншоте)?**
- Открыт полностью — критично для точного понимания, что именно триггерит каждый интент; без полного текста нельзя оценить полноту покрытия синонимов.

**В: Что произойдёт при создании новой политики через «+ Политика» — какие поля обязательны?**
- A. Те же поля, что видны в таблице: название, regex, класс (payload), действия «в интенте»/«вне интента»
- B. Есть дополнительные скрытые поля (приоритет применения политик друг относительно друга, например)

**В: Идентичны ли regex-паттерны `код-процедур-шум` и `процедура-документация-шум`?**
- A. Да, один и тот же триггер интента применяется к двум разным payload-классам (`is_code` и `procedural`) осознанно — интент «про процедуры» единый, а полезность самого контента разная (код vs документация к нему)
- B. Нет, совпадение — визуальный артефакт обрезки длинного текста на скриншоте

**В: Есть ли отдельный механизм реланирования для **ошибок выполнения** SQL (не только «низкой уверенности retrieval»)?** [важно для Кейса 01]
- A. Нет отдельного механизма — именно поэтому в Кейсе 01 агент не переисправил упавший SQL, а сдался
- B. Механизм есть, но не был задействован в конкретном случае Кейса 01 по другой причине
- C. Обработка ошибок SQL делегирована самой LLM (перегенерировать при ошибке), без отдельной инфраструктуры реланирования

**В: Какие ещё категории роутинга существуют, кроме «Data-вопросы» и «Структурные вопросы»?**
- A. Отдельная категория для документных/текстовых вопросов
- B. Отдельная категория для кода (учитывая находки про GitLab-интеграцию и `is_code`)
- C. Отдельная категория для email/переписки (раз это Must Have в ТЗ)

**В: Что скрыто во «Встроенных правилах (зашиты в код, только просмотр)» — сколько их и какие категории?**
- Открыт полностью — блок специально свёрнут по умолчанию в UI, содержимое не наблюдалось.

---

## Калибровка → Калибровка детектора / Отладка сессий

> См. [`kalibrovka-detektora-i-otladka.md`](kalibrovka-detektora-i-otladka.md)

**В: Что содержит вкладка «Корни measure» и как «range-роутер» соотносится с «Роутингом вопросов»?**
- A. Range-роутер — более узкий предшественник общего «Роутинга вопросов», отвечает только за числовые диапазоны/меры (например, «сколько», «больше X»)
- B. Независимый параллельный механизм, специфичный именно для табличных/числовых данных, не связанный с общим роутингом фраз/regex

**В: Какие данные пишутся в аудит-БД, если её сконфигурировать?** [важно для Кейса 01]
- A. Полный technical reasoning log: сгенерированный SQL, найденные чанки, план — то, чего не хватило при разборе Кейса 01
- B. Только базовые метаданные (пользователь/время/модель), без детализации до уровня SQL
- C. Настраиваемый уровень детализации логирования

**В: Что делает вкладка «Обслуживание» в калибровке детектора?**
- A. Ручной запуск переиндексации/техобслуживания коллекции
- B. Просмотр логов/состояния детектора
- C. Очистка устаревших данных калибровки

**В: Что означают «оценки» диалогов, упомянутые в описании «Отладки сессий»?**
- A. Пользовательские оценки ответов (лайк/дизлайк, как в большинстве чат-продуктов)
- B. Автоматические оценки качества (confidence, relevance) от самой системы
- C. Оценки от куратора/QA-команды, выставленные вручную при ревью

---

## Управление данными

> См. [`upravlenie-dannymi.md`](upravlenie-dannymi.md)

**В: Что такое «ОМ» и почему в примере «нет сервисов в ОМ»?**
- A. «Объектная модель» — реестр объектов данных (таблиц, процедур) по всей организации
- B. «Операционная модель» — реестр сервисов/окружений, куда подключаются коллекции
- Уверенно то, что это реестр целевых сервисов/БД, к которому привязываются код-коллекции и источники для графа процедур — сама расшифровка аббревиатуры не подтверждена.

**В: Как выглядит заполненный граф процедур — визуализация связей или только метрики покрытия?**
- A. Полноценная интерактивная визуализация графа (узлы/рёбра)
- B. Только табличные метрики покрытия, без графической визуализации, несмотря на название «граф»

**В: Что покажет «Анализ схем» после запуска — список колонок или сгенерированные текстовые описания?**
- A. Список колонок с типами (сырая схема)
- B. Сгенерированные текстовые описания полей для использования в RAG-контексте (более вероятно, учитывая назначение — поддержка text-to-SQL)

**В: Как соотносятся «Граф процедур», «Граф код→таблица» и «Граф доступа» — разные грани одной lineage-системы или независимые инструменты?**
- A. Единая lineage-система с тремя проекциями: процедуры, код→таблицы, права доступа
- B. Независимые инструменты, использующие общий движок «ОМ», но не связанные напрямую друг с другом

**В: Что покажет «Карта покрытия» в «Графе доступа» — список незащищённых таблиц/колонок?**
- A. Именно так — таблицы/колонки без назначенных row-политик или масок
- B. Наглядная визуализация (тепловая карта) покрытия по всем персонам сразу

**В: Реально ли настроены row-фильтры/маски хотя бы для одной персоны, или это тоже пустая конфигурация?** [критично для комплаенса]
- A. Настроены и работают — просто на скриншоте персона не выбрана для показа
- B. Экран существует, но конфигурация пуста (аналогично незаполненной «Аудит-БД» в «Отладке сессий»)

**В: Почему в дропдауне «Коллекция» (Граф код→таблица) по умолчанию выбраны «Сохранённые ответы (шаблоны)»?**
- A. Осмысленный выбор — шаблоны действительно синхронизируются из GitLab / участвуют в lineage
- B. Случайность — первое/последнее использованное значение в дропдауне, не несёт смысла

---

## Агенты

> См. [`agenty.md`](agenty.md)

**В: Чем отличаются «системный» и «канвасный» варианты одного типа агента (пример — Closed-book RAG)?**
- A. Только формой конфигурации (canvas-flow vs системный промпт) при идентичном итоговом поведении
- B. Разное поведение — канвасный вариант допускает более сложную логику/ветвление, системный — простой промпт

**В: Что показывает «Реестр агентов» в отличие от «Активных агентов»?**
- A. Все агенты, включая неактивные/архивные версии
- B. Полная история версий каждого агента (согласуется с фразой «редактировать на канвасе как новую версию»)

**В: Как выглядит сам «канвас» — визуальный редактор графа обработки, какие узлы доступны?**
- Открыт полностью — не увиден ни на одном скриншоте; ожидаемо содержит узлы вроде «источник данных», «политика доступа», «модель», «промпт» и т.п., по аналогии с другими no-code agent-builder продуктами, но это предположение.

---

## Администрирование

> См. [`administrirovanie-polzovateli.md`](administrirovanie-polzovateli.md)

**В: Что даёт роль «Администратор (все провайдеры)» на странице «Пользователи и доступ» сверх роли «— без роли —»? — Решено (проверено на практике, 12.08.2026)**
- Ничего сверх набора доступных LLM-провайдеров (вариант A из первоначальной постановки). Это подтвердилось косвенно: доступ в саму админку («Рабочее место ИБ») управляется отдельным, третьим механизмом — «Сотрудники ИБ» (`/dlp/staff/`, своя роль «Администратор»/«Оператор ИБ», привязка к identity через поле «OM-email»), не связанным с ролью на странице «Пользователи и доступ». Вариант B не подтвердился.

**В: Как связаны Каталог (OpenMetadata), «Пользователи и доступ» (`/dlp/users/`) и доступ в саму админку?**
- Уточнено (12.08.2026): пользователь, созданный как Admin в Каталоге, может входить в чат платформы и затем сам появляется на `/dlp/users/` — но это не даёт доступа в саму админку («Рабочее место ИБ»). Для входа в админку нужен третий, отдельный шаг — запись в «Сотрудники ИБ» (`/dlp/staff/`) с ролью «Администратор» и тем же email в поле «OM-email». Остаётся не до конца проверенным: появляется ли запись на `/dlp/users/` автоматически сразу при первом входе пользователя в платформу, или с задержкой — момент первого входа отдельно не зафиксирован.

---

## База секретов

> См. [`baza-sekretov.md`](baza-sekretov.md)

**В: На экране «Regex-паттерны» — выпадающий список «Тип токена» ограничен существующими встроенными `PIIType`, или там можно завести принципиально новый тип с собственным префиксом? — Решено (скриншот раскрытого списка, 17.08.2026)**
- Ни то, ни другое из первоначальных вариантов. Список закрытый (не текстовое поле), но состоит всего из 5 значений: `PERSON`, `PROJECT`, `COMPANY`, `PRODUCT`, `CUSTOM` — это НЕ те же 20 `PIIType` из документа разработчика (там нет `PROJECT`/`PRODUCT`, а `ORG`/`COMPANY` — разные имена) и не свободный ввод произвольного префикса. `CUSTOM`, судя по всему, единый catch-all для всего, что не подходит под первые четыре категории — то есть у своего паттерна нет отдельного, узнаваемого префикса маски (типа `[CONTRACT_N]`), только общий `[CUSTOM_N]`.

**В: Совпадает ли механизм `custom_patterns` / `GET /api/v1/shield-config` с описанным в документе разработчика механизмом `CustomPIIType` (config.py:151-200 + Admin API), или это два разных пути добавления кастомных паттернов? — Частично прояснено**
- Похоже на вариант B: `custom_patterns` через экран «Regex-паттерны» — заметно более простой/ограниченный путь (5 фиксированных категорий, нет полей prefix/display_name/валидатор), чем `CustomPIIType` из документа разработчика (свободный `id`/`prefix`/`display_name` + опциональный валидатор `luhn`/`inn`/`ogrn` и т. д.). Остаётся не до конца ясным: это два независимых механизма в системе, или UI-экран — упрощённая надстройка над тем же самым `CustomPIIType`, где поле `prefix` на бэкенде просто зафиксировано этими пятью значениями в данном развёртывании. Стоит уточнить у команды платформы напрямую.

**В: Что показывают неописанные пункты меню «Список документов», «Загрузить», «Загрузить блэклист»?**
- Открыто полностью — не увидено ни на одном скриншоте.

---

## Как использовать этот реестр

Более практично — сверить каждый пункт с реальными владельцами платформы (командой D8/«Платформа») напрямую, а не гадать дальше по скриншотам. Вопросы сгруппированы по экрану специально, чтобы можно было переслать конкретный блок ответственному за конкретный раздел.
